<% if (site.tags.length){ %>
  <div class="widget-wrap">
    <h3 class="widget-title"><%= __('tags') %></h3>
    <div class="widget">
      <!-- <%- list_tags({show_count: theme.show_count}) %> -->
      <%- list_tag_tree({show_count: theme.show_count, root_tag_option: theme.root_tag_option}) %>
    </div>
  </div>
<% } %>
